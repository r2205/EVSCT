<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".EvsctApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EVSCT">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <!-- adjustResize, not the adjustUnspecified default: some OEM
             builds resolve unspecified to adjustPan for Compose windows
             (no View-level scroll containers to resize), which pans the
             whole window — top bar included — off-screen while typing.
             With edge-to-edge the window never actually resizes; this
             only pins it in place so WindowInsets.ime handling in the
             screens does the moving. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.EVSCT"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Chosen-component callback for the backup share sheet: records
             the "last backed up" timestamp only when the user actually
             picks a share target (cancelling the sheet fires nothing). -->
        <receiver
            android:name=".data.backup.BackupShareChosenReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- EvsctApplication supplies WorkManager's Configuration on-demand
             (Configuration.Provider) so the Hilt worker factory is wired in.
             That requires removing WorkManager's default auto-initializer from
             androidx.startup, otherwise it double-initializes and lintVital
             fails the release build (RemoveWorkManagerInitializer). -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
