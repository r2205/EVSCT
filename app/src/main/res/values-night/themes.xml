<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Night twin of values/themes.xml: the dark platform parent, so any
         framework-drawn chrome defaults dark, plus the same night-qualified
         window background. -->
    <style name="Theme.EVSCT" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
