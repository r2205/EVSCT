[versions]
agp = "9.3.0"
# Drives the Compose compiler plugin, which pulls kotlin-gradle-plugin of the
# same version onto the buildscript classpath. That is the compiler AGP's
# built-in Kotlin then uses, so this one ref keeps the Compose compiler and the
# Kotlin compiler in lockstep — they have to match exactly.
kotlin = "2.3.21"
# 2.3.3 moved off the deprecated KGP compilerOptions API and 2.3.10 fixed
# R-class resolution under AGP 9 built-in Kotlin; both are required here.
ksp = "2.3.10"
# 2.59 added AGP 9 support to the Hilt Gradle plugin; 2.60 rebuilt Hilt on
# Kotlin 2.3.21, matching the compiler that now processes its generated code.
hilt = "2.60.1"
androidxHilt = "1.2.0"
hiltNavCompose = "1.2.0"
coreKtx = "1.15.0"
lifecycle = "2.8.7"
activityCompose = "1.9.3"
# Compose 1.11.4 / Material3 1.4.0. Stays below Compose 1.12, which is the
# release that starts requiring compileSdk 37.
composeBom = "2026.06.01"
navigationCompose = "2.8.5"
room = "2.8.4"
coroutines = "1.9.0"
poi = "5.3.0"
datastore = "1.1.1"
coil = "2.7.0"
playServicesMaps = "19.0.0"
mapsCompose = "4.4.1"
junit = "4.13.2"
work = "2.10.0"
robolectric = "4.14.1"
androidxTestExtJunit = "1.2.1"

[libraries]
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
androidx-lifecycle-runtime-ktx = { group = "androidx.lifecycle", name = "lifecycle-runtime-ktx", version.ref = "lifecycle" }
androidx-lifecycle-runtime-compose = { group = "androidx.lifecycle", name = "lifecycle-runtime-compose", version.ref = "lifecycle" }
androidx-lifecycle-viewmodel-compose = { group = "androidx.lifecycle", name = "lifecycle-viewmodel-compose", version.ref = "lifecycle" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }
androidx-compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "composeBom" }
androidx-compose-ui = { group = "androidx.compose.ui", name = "ui" }
androidx-compose-ui-graphics = { group = "androidx.compose.ui", name = "ui-graphics" }
androidx-compose-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
androidx-compose-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
androidx-compose-material3 = { group = "androidx.compose.material3", name = "material3" }
# Still resolved by the BOM, but frozen: androidx stopped publishing the icons
# libraries after 1.7.8 in favour of Material Symbols vector drawables, and
# Material3 1.4.0 dropped its transitive dependency on material-icons-core.
# Declaring -extended explicitly is what still pulls core in. The icons keep
# working; they just don't gain new ones as the BOM moves.
androidx-compose-material-icons-extended = { group = "androidx.compose.material", name = "material-icons-extended" }
androidx-navigation-compose = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigationCompose" }

androidx-room-runtime = { group = "androidx.room", name = "room-runtime", version.ref = "room" }
androidx-room-ktx = { group = "androidx.room", name = "room-ktx", version.ref = "room" }
androidx-room-compiler = { group = "androidx.room", name = "room-compiler", version.ref = "room" }

androidx-datastore-preferences = { group = "androidx.datastore", name = "datastore-preferences", version.ref = "datastore" }

hilt-android = { group = "com.google.dagger", name = "hilt-android", version.ref = "hilt" }
hilt-compiler = { group = "com.google.dagger", name = "hilt-compiler", version.ref = "hilt" }
hilt-navigation-compose = { group = "androidx.hilt", name = "hilt-navigation-compose", version.ref = "hiltNavCompose" }
androidx-hilt-work = { group = "androidx.hilt", name = "hilt-work", version.ref = "androidxHilt" }
androidx-hilt-compiler = { group = "androidx.hilt", name = "hilt-compiler", version.ref = "androidxHilt" }
androidx-work-runtime-ktx = { group = "androidx.work", name = "work-runtime-ktx", version.ref = "work" }

kotlinx-coroutines-android = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-android", version.ref = "coroutines" }

apache-poi = { group = "org.apache.poi", name = "poi", version.ref = "poi" }
apache-poi-ooxml = { group = "org.apache.poi", name = "poi-ooxml", version.ref = "poi" }

coil-compose = { group = "io.coil-kt", name = "coil-compose", version.ref = "coil" }

play-services-maps = { group = "com.google.android.gms", name = "play-services-maps", version.ref = "playServicesMaps" }
maps-compose = { group = "com.google.maps.android", name = "maps-compose", version.ref = "mapsCompose" }
maps-compose-utils = { group = "com.google.maps.android", name = "maps-compose-utils", version.ref = "mapsCompose" }

junit = { group = "junit", name = "junit", version.ref = "junit" }
# Spelled out rather than via the kotlin("test") DSL helper: that helper produces
# a versionless "org.jetbrains.kotlin:kotlin-test", relying on the kotlin-android
# plugin to constrain it, and nothing applies that plugin any more.
kotlin-test = { group = "org.jetbrains.kotlin", name = "kotlin-test", version.ref = "kotlin" }

# JVM-side Compose UI tests: ui-test-junit4 drives composables, Robolectric
# stands in for the device, ui-test-manifest registers the host activity.
# The two Compose artifacts take their version from the BOM.
androidx-compose-ui-test-junit4 = { group = "androidx.compose.ui", name = "ui-test-junit4" }
androidx-compose-ui-test-manifest = { group = "androidx.compose.ui", name = "ui-test-manifest" }
robolectric = { group = "org.robolectric", name = "robolectric", version.ref = "robolectric" }
androidx-test-ext-junit = { group = "androidx.test.ext", name = "junit", version.ref = "androidxTestExtJunit" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
# No kotlin-android entry: AGP 9's built-in Kotlin compiles our sources, and
# Kotlin 2.3 makes applying org.jetbrains.kotlin.android alongside AGP 9 an error.
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }
hilt = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }
